# Type of data structure
# kind of same with list
# tuples have parenthises ()

coordinates = (4, 5)

#tuples are immutable
print(coordinates[0]) # accessing elements with index

#use tuples for data that is never going to change

# you can make a list of tuples

coordinates3 = [(4, 5), (6, 7), (8, 9)]

